# 0.9.0 release verification / 发布验收

Date: 2026-09-17

## Baseline

Compared against the published GitHub v0.8.9 release, its downloaded JARs, and source tags `v0.8.9` / `v0.8.9-1.20.1`.

- 1.21.1 baseline SHA-256: `5b4a28c49dfaf08217ca8b51760a948fad405281df5fb89bf21159325ebfb2a7`
- 1.20.1 baseline SHA-256: `063ba6ec3f1e264ae3d368a6ffc37d858396ec71b5489d944c8607ade783e390`

## Automated checks

- Minecraft 1.21.1 / NeoForge: 220 tests, 219 passed, 0 failures, 1 pre-existing conditional skip.
- Minecraft 1.20.1 / Forge: 221 tests, 220 passed, 0 failures, 1 pre-existing conditional skip.
- The skipped test in each branch requires an external `../SuperFactoryManager/examples` checkout. Bundled SFML fixture and bilingual example tests did run.
- Real editor handlers exercised for both scrollbar axes, dragging outside the pane, release/cancellation, resize preservation, and replace-all undo/redo across wrapping and resizing.
- Layout, Unicode wrapping, literal search/replacement, source limits, compiler error positions, translations, and existing model/compiler regression suites passed.
- Release JAR checks: ZIP integrity, internal version 0.9.0, Java bytecode levels, editor classes, translation keys/placeholders, English text, declared mixins, required bundled dependencies.
- Forge production JAR: relocated PinIn and dictionary loaded and matched full pinyin and initials; original and relocated PinIn packages resolved together in a Java module layer. This checks the package conflict behind the earlier Just Enough Characters startup issue; it is not a full modpack launch test.

## Build commands

```text
# main (1.21.1): uses already-prepared Minecraft dependency outputs
./gradlew test jarJar --offline -x neoFormPackRecomp -x create1.21.1ClientExtraJar
# Distribute build/libs/SFM-Factory-Studio-1.21.1-0.9.0.jar, not the slim JAR.

# 1.20.1: includes Forge production remapping and automatic PinIn relocation
./gradlew test build --offline
# Distribute build/release/SFM-Factory-Studio-1.20.1-0.9.0.jar.
# build/libs/*-unrelocated.jar is an intermediate artifact.

python scripts/verify_release.py <release.jar> --minecraft <1.20.1|1.21.1> --version 0.9.0
# Forge branch additionally provides scripts/PinyinReleaseSmoke.java.
```

The broader NeoForge `build` task also attempted to download missing game runtime assets and could not finish offline. Mod compilation, all tests, and the bundled production `jarJar` task completed successfully. The two excluded tasks regenerate cached Minecraft dependencies; they do not skip addon compilation or tests.

## Manual verification boundary / 人工验收范围

No Minecraft client was running during this release session. In-game visual, IME, and full-modpack acceptance were not completed. Automated checks do not establish zero defects. The window-resize button fix was reviewed and compiled in both branches.

本次未完成游戏内视觉、中文输入法及完整整合包实测；不将自动测试通过表述为零缺陷保证。中英文更新日志见同目录 `0.9.0.md`。
